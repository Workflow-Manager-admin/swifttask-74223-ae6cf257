# To start the Flask backend server (so the frontend can connect):

cd todo_api_backend_workspace/todo_api_backend
source venv/bin/activate
export FLASK_ENV=development
export FLASK_APP=run.py
flask run

# You can now connect your frontend app to: http://127.0.0.1:5001/
